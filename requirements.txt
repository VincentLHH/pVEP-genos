# Core inference
torch
transformers>=4.36.0
accelerate

# I/O
pysam

# Sequence reconstruction (alternative to built-in builder)
genvarloader

# API server (GPU 节点部署服务时需要)
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# API client (无 GPU 节点调用远程服务时需要)
httpx>=0.27.0

# Pipeline utilities
pyyaml
tqdm
