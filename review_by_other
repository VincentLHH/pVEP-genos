 严格审查报告：pVEP-genos 模型训练框架

  整体架构分两套实现（apps/main.py 和 apps/ml/run_ml.py），以下逐一审查三大关注点。

  ---
  一、数据泄露与交叉验证可靠性

  已做好的（无需修改）

  1. 预处理在 fold 内 fit — 两套实现都正确：
    - apps/models/trainer.py:139-142 — 每 fold 新建 CombinedPreprocessor，仅 fit(X_train)
    - apps/ml/trainer.py:159-165 — _preprocess_fold 仅在训练部分 fit
  2. StratifiedKFold + shuffle — 保证类别比例一致
  3. OOF 预测收集 — 每 fold 的 val 索引互不重叠，无标签泄露

  发现的问题（需修复）

  问题 1（严重）：apps/ml/trainer.py 超参搜索存在双重泄露

  MLTrainer.cv_evaluate() 第 168-186 行：

  for params in param_grid:
      model = create_model(model_name, random_state=self.random_state, **params)
      model.fit(X_train_proc, y_train)
      if scoring == "roc_auc":
          score = roc_auc_score(y_val, model.predict_proba(X_val_proc)[:, 1])

  问题：用验证集选超参，但最终 OOF 预测用的是验证集上最优的模型，这是标准做法没错。但更关键的是 — StandardizableModel.fit()
  内部又做了一次 StandardScaler.fit_transform(X) (apps/ml/models.py:49-50)，这会导致：

  - 预处理器已经对数据做了标准化/PCA
  - 模型内部又做了一次标准化

  这不是泄露，但是做了重复标准化，对 SVM/LR/MLP 会影响结果。

  问题 2（严重）：apps/ml/trainer.py 预处理器在 fold 间复用

  MLTrainer._get_preprocessor() 第 73-87 行：

  def _get_preprocessor(self):
      if self._preprocessor is None:
          self._preprocessor = MultiOmicsPreprocessor(...)
      return self._preprocessor

  self._preprocessor 是实例属性，第一次调用后缓存。但 _preprocess_fold 每次调用 preprocessor.fit(X_train)，这会覆盖之前的
  fit 状态。虽然每个 fold 重新 fit，但 MultiOmicsPreprocessor.fit() 中 _infer_column_indices() 会修改
  self.emb_feature_indices 和 self.tab_feature_indices（第 266-277 行），如果 feature_names 在不同 fold
  不变，这不是问题。但更重要的是 — 在 cv_evaluate 最后（第 240-242 行）：

  if self._preprocessor is not None:
      self._preprocessor.fit(X, feature_names=feature_names)  # 用全量数据 fit
      X_proc = self._preprocessor.transform(X)

  这是训练最终模型用的，用全量数据 fit 预处理器是正确的（最终模型要用全部数据），但这个 _preprocessor 和 CV
  过程中复用同一个对象，可能在多线程或后续调用中产生问题。设计上应该用独立的预处理器。

  问题 3（中等）：apps/models/classifiers.py 的 SklearnClassifier 内置 Scaler 与 Trainer 预处理重叠

  apps/models/classifiers.py:96-99：

  if self.need_scaling:
      self.scaler = StandardScaler()
      X = self.scaler.fit_transform(X)

  当 need_scaling=True（lr, svm, mlp），模型内部又做了一次标准化。而 CombinedPreprocessor 已经对 emb 和 table
  分别做了标准化。结果：数据被标准化了两次。

  这不是泄露，但会导致 SVM/LR/MLP 的特征分布被过度压缩，影响模型性能。

  问题 4（中等）：apps/main.py PCA 散点图在 CV 前对全量数据做了 PCA

  apps/main.py:123：

  plot_pca_scatter(X, y, save_path=str(output_dir / "pca_plot.png"))

  plot_pca_scatter 内部会 fit PCA，这是全量数据的 PCA，用于可视化是可以接受的，但需确认不影响后续训练流程（只读
  X，不修改）。

  问题 5（低）：超参搜索方式是手动网格搜索，不是 sklearn 的 GridSearchCV

  两套实现都是手动遍历参数网格，在验证集上直接评估。这意味着：
  - 没有嵌套交叉验证
  - 超参搜索的分数直接用验证集评估，有乐观偏差

  不过对于小数据集场景，5-fold CV + 手动网格搜索是常见做法，问题不大。

  ---
  二、标签混淆与多模态数据区分

  已做好的

  1. 样本 ID 严格对齐 — 两套实现都通过 sample_id 交集对齐，确保 emb/table/label 一一对应
  2. 特征名前缀区分 — emb_、metab_、pheno_ 前缀明确区分模态
  3. CombinedPreprocessor 分开处理 — emb 和 table 有独立的预处理器

  发现的问题

  问题 6（严重）：apps/ml/data_loader.py 的 _load_emb 只用 Mut_hap1 做特征，丢弃了其他 5 个向量

  apps/ml/data_loader.py:213-229：

  emb = next(iter(models_dict.values()))
  mut_hap1 = emb.get("Mut_hap1", [])
  if not mut_hap1:
      continue
  variant_data.append((emb, np.asarray(mut_hap1, dtype=float)))
  ...
  top_embs = self._select_top_variants(variant_data)
  if self.cfg.emb_aggregation == "max":
      sample_emb = np.max(top_embs, axis=0)
  else:
      sample_emb = np.mean(top_embs, axis=0)

  这里 只用了 Mut_hap1 向量 作为特征，WT_hap1、Mut_hap2、WT_hap2、Mut_ref、WT_ref 全部丢弃。但 apps/main.py 那条线路（通过
  aggregation.py）正确使用了全部 6 个向量（concat/sum/mean）。

  这意味着 apps/ml/ 路线的信息量严重不足，和 apps/main.py 路线的结果不可比。

  问题 7（中等）：TableImputer 用全局中位数而非每列中位数

  apps/ml/preprocessor.py:132-137：

  def fit(self, X, y=None):
      if self.strategy == "median":
          self.fill_value_ = float(np.nanmedian(X))

  对所有列使用同一个全局中位数填补，而非每列独立计算。相比之下，apps/data/preprocess.py 的 TablePreprocessor
  正确地每列独立计算中位数。

  不同特征的量纲和分布差异很大，用全局中位数填补会引入偏差。

  问题 8（中等）：代谢组和表型组在预处理中被合并处理，无法分别施加不同策略

  MultiOmicsPreprocessor 将所有非 emb_ 前缀的列统一作为 tab 处理，使用相同的
  TableImputer。代谢组和表型组可能有不同的缺失模式，应该可以分别配置。

  问题 9（低）：消融实验中 emb_no_background 特征组的索引不正确

  apps/main.py:320-345，emb_no_background 重新聚合后返回新的特征矩阵，但 emb_indices 被设为
  np.arange(X_bg.shape[1])，即所有列都被当作 emb。这对预处理器的行为有影响 — 全部列会走 PCA 降维路径，而非 table
  填补路径。如果这些特征本应该是 table 特征，则会有问题。不过从逻辑上看，ref embedding 确实属于 emb 类，所以目前是正确的。

  ---
  三、可重复性

  已做好的

  1. seed_everything() — 设置 random、numpy、PYTHONHASHSEED、torch 种子
  2. StratifiedKFold(random_state=42) — 数据分割可重复
  3. PCA(random_state=42) — 降维可重复
  4. sklearn 模型传 random_state — LR/SVM/RF/XGB/MLP 都有种子

  发现的问题

  问题 10（严重）：TorchClassifier 的 DataLoader 使用 shuffle=True 但未设置 worker seed

  apps/models/classifiers.py:230：

  loader = DataLoader(dataset, batch_size=self.batch_size, shuffle=True)

  DataLoader 的 shuffle 依赖 PyTorch 的随机状态。虽然 seed_everything 设置了 torch.manual_seed，但：
  - 如果在多 fold 训练中，PyTorch 的随机状态会被每个 fold 的训练消耗，导致不同 fold 间不独立
  - torch.backends.cudnn.deterministic = True 在 seed_everything 中设置了，但 TorchClassifier 没有设置
  torch.use_deterministic_algorithms(True)

  问题 11（中等）：apps/ml/run_ml.py 的 setup_random_seed 不如 apps/utils.py 的 seed_everything 完整

  apps/ml/run_ml.py:117-136：

  def setup_random_seed(seed: int):
      np.random.seed(seed)
      random.seed(seed)
      os.environ["PYTHONHASHSEED"] = str(seed)
      torch.manual_seed(seed)
      torch.cuda.manual_seed_all(seed)

  缺少：
  - torch.backends.cudnn.deterministic = True
  - torch.backends.cudnn.benchmark = False

  问题 12（低）：XGBoost 的 n_jobs=-1 可能影响可重复性

  XGBoost 使用多线程时，浮点运算的累积顺序可能不同，导致结果在不同硬件上不完全一致。这是 XGBoost 的已知行为。

  ---
  修复建议汇总

  ┌─────┬────────┬────────────────────┬─────────────────────────────┬───────────────────────────────────────────────────┐
  │  #  │ 严重度 │        文件        │            问题             │                     修复方案                      │
  ├─────┼────────┼────────────────────┼─────────────────────────────┼───────────────────────────────────────────────────┤
  │ 3   │ 严重   │ classifiers.py     │ SklearnClassifier 内置      │ need_scaling 应设为 False，因为 Trainer           │
  │     │        │                    │ Scaler 与预处理器重复标准化 │ 的预处理器已经做了标准化                          │
  ├─────┼────────┼────────────────────┼─────────────────────────────┼───────────────────────────────────────────────────┤
  │ 6   │ 严重   │ ml/data_loader.py  │ 只用 Mut_hap1，丢弃 5       │ 参考 apps/main.py 路线，使用全部 6 向量聚合       │
  │     │        │                    │ 个向量                      │                                                   │
  ├─────┼────────┼────────────────────┼─────────────────────────────┼───────────────────────────────────────────────────┤
  │ 7   │ 中等   │ ml/preprocessor.py │ TableImputer 全局中位数     │ 改为每列独立计算中位数                            │
  ├─────┼────────┼────────────────────┼─────────────────────────────┼───────────────────────────────────────────────────┤
  │ 2   │ 中等   │ ml/trainer.py      │ 预处理器在 CV               │ 最终模型训练时用独立的预处理器实例                │
  │     │        │                    │ 和最终模型间复用            │                                                   │
  ├─────┼────────┼────────────────────┼─────────────────────────────┼───────────────────────────────────────────────────┤
  │ 11  │ 中等   │ ml/run_ml.py       │ setup_random_seed 不完整    │ 补充 cudnn deterministic 和 benchmark 设置        │
  ├─────┼────────┼────────────────────┼─────────────────────────────┼───────────────────────────────────────────────────┤
  │ 10  │ 中等   │ classifiers.py     │ TorchClassifier DataLoader  │ 添加                                              │
  │     │        │                    │ 可重复性不足                │ generator=torch.Generator().manual_seed(seed)     │
  │     │        │                    │                             │ 和确定性算法设置                                  │
  ├─────┼────────┼────────────────────┼─────────────────────────────┼───────────────────────────────────────────────────┤
  │ 8   │ 低     │ ml/preprocessor.py │ 代谢组/表型组合并处理       │ 可选：拆分为独立预处理器                          │
  └─────┴────────┴────────────────────┴─────────────────────────────┴───────────────────────────────────────────────────┘
